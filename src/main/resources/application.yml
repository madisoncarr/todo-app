spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todos
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: password

logging:
  level:
    org:
      springframework:
        security: DEBUG
